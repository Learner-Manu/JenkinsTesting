pipeline {
  agent any
  stages {
    stage('scm') {
      steps {
        echo 'scm'
      }
    }
    stage('build') {
      steps {
        echo 'build'
      }
    }
    stage('Quality') {
      steps {
        echo 'build'
      }
    }
    stage('Dockerize') {
      steps {
        echo 'dockerize'
      }
    }
    stage('Dockerize') {
      steps {
        echo 'dockerize'
      }
    }

    stage('Deploy site') {
      steps {
        echo 'deploy'
      }
    }
  }
}